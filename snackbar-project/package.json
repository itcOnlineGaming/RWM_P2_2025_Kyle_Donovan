{
  "name": "snackbar-project",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=demo",
    "build": "npm run build --workspace=packages/snackbar",
    "package": "npm run package --workspace=packages/snackbar"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10"
  }
}
